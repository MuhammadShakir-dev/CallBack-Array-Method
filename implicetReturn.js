// Implicet Return is the more shoter way and smater way to make functions expressions in javascript.
// NOTE:
// Implicet Return works only when there is only one value that you want to return.

const leapyear = (year) => (
          year % 4 == 0
    )
    
const add = (a,b) => (a+b);

const add = (a,b) => a+b;

